# ADR-002：结构化文件是事实源

状态：Proposed

## 决策

使用版本化 JSON 保存领域事实，保留原始附件和必要 Markdown；HTML 仅作为可重建的展示投影。

## 原因

- HTML 适合非 coder 阅读，但不适合可靠存储状态。
- 结构化数据可以验证、比较、迁移和重新渲染。
- AI 输出必须经过 Schema 校验，避免自由文本成为隐式数据库。

## 结果

- Workbench 丢失后可以完整重建。
- Schema 变更必须提供版本和迁移路径。
- 用户不应被要求手工编辑内部 JSON。
